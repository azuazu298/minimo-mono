import React from "react";
import ReactDOM from "react-dom/client";
import MinimoMono from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MinimoMono />
  </React.StrictMode>
);
